# Ember — Supabase project configuration.
# Used by the Supabase CLI for local development and migrations.
# See: https://supabase.com/docs/guides/cli

[project]
id = ""  # Fill in after `supabase link`
name = "ember"

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
enabled = true
port = 54323

[storage]
enabled = true
file_size_limit = "10MB"

[auth]
enabled = false  # No auth for initial prototype — add later
